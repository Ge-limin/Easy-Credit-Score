# TODO: load model, pass in user_info (preprocessing as needed), 
# and return 0 (poor) or 1 (good) as prediction

def compute_model_prediction(user_info):
    return 0.5*1

# TODO: may need to normalize B,C,D category weights in the case that their info is not available

def compute_rental_score(rental_info):
    return 0.2*1

def compute_utility_score(utility_info):
    return 0.2*1

def compute_landlordref_score(landlord_ref):
    return 0.1*1